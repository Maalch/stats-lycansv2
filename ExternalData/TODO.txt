Fix src\config\chartConstants.ts : 
- why is there 2 struct MIN_GAMES_DEFAULTS and CHART_DEFAULTS?
- be sure that the default values match a values from MIN_GAMES_OPTIONS. Is there a way to make sure that this is always the case?

Move "Répartition des Couleurs par Joueur" into "Classements"?